name: offline_ml_pipeline_example
description: Example app demonstrating offline_ml_pipeline usage.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  offline_ml_pipeline:
    path: ../
  file_picker: ^6.1.1
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
